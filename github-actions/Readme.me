## Manual Trigger

```sh
gh workflow run greet.yml -f name=mona -f greeting=hello -F data=@myfile.txt
echo '{"name":"mona", "greeting":"hello"}' | gh workflow run greet.yml --json
```

## Webhook Event
 
```sh
curl -X POST --location "https://api.github.com/repos/KirillMakrutin/Github-Examples/dispatches" \
    -H "Accept: application/vnd.github+json" \
    -H "Authorization: token {classic access token for workflow}" \
    -H "Content-Type: application/json" \
    -d '{
          "event_type": "webhook",
          "client_payload": {
            "key": "value",
            "keyB": "valueB"
          }
        }'
```